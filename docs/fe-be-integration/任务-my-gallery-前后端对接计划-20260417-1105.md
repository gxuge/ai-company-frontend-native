# Task: my-gallery FE-BE Integration

- Page: `src/app/pages/my-gallery/index.tsx`
- Created at: `2026-04-17 11:05`
- Status: `completed`
- Goal: replace local mock gallery data with backend data and connect delete flow, without changing existing UI layout structure.

## 1. Confirmed APIs

- List assets: `GET /sys/ts-user-image-assets`
- Delete asset: `DELETE /sys/ts-user-image-assets?id=xxx`

## 2. Field Mapping

- `image.id` <- `id`
- `image.url` <- `thumbnailUrl` (fallback `fileUrl`)
- `image.name` <- `fileName` (fallback `Image {id}`)
- `image.type` <- `sourceType` (mapped labels, fallback `Uncategorized`)
- Selection/manage mode states <- local page state only

## 3. Task Breakdown

| Task | Status | Description | Acceptance |
| --- | --- | --- | --- |
| T1 API wrapper completion | completed | Add asset delete method in `src/lib/api/ts-role-image.ts` | Page does not call raw URL |
| T2 Gallery list integration | completed | Replace `initialImages` mock with real API data | Page renders backend assets |
| T3 Delete flow integration | completed | Manage mode delete calls backend and updates list | Deleted assets removed from list |
| T4 Loading/error/empty fallback | completed | Add loading, error message and empty state fallback | No white screen on failures |
| T5 Verification and docs | completed | Run targeted lint and backfill docs | Command evidence and records updated |

## 4. Risks and Rollback

- Risks:
  - Some historical assets may miss `thumbnailUrl` or `fileName`.
  - Partial delete failures may happen in batch operation.
- Rollback:
  - Keep layout unchanged and show error fallback text when requests fail.
  - If needed, rollback only `my-gallery` page and `ts-role-image` API wrapper changes.

## 5. Execution Result

- Frontend API update:
  - Added `tsRoleImageApi.deleteUserImageAsset(id)`.
- Page integration:
  - Load all user assets by paging.
  - Map backend fields to card fields.
  - Keep selection/use/manage UI behavior.
  - Delete selected assets with backend API and local state refresh.
- Empty state action:
  - "Back to Create Character" now navigates to `/pages/create-character`.
- Documentation backfill:
  - Updated `docs/fe-be-integration/integration-status.md`
  - Updated `docs/plan/plan.md`
  - Updated `docs/changelog/changelog.md`

## 6. Evidence Index

- Frontend files:
  - `src/app/pages/my-gallery/index.tsx`
  - `src/lib/api/ts-role-image.ts`
- Docs files:
  - `this task file (current file)`
  - `docs/fe-be-integration/integration-status.md`
  - `docs/plan/plan.md`
  - `docs/changelog/changelog.md`
- Backend references read:
  - `TsUserImageAssetController.java`
  - `TsUserImageAssetServiceImpl.java`
  - `TsUserImageAssetMapper.xml`
- Validation:
  - `pnpm exec eslint src/app/pages/my-gallery/index.tsx src/lib/api/ts-role-image.ts --rule "unicorn/filename-case: off" --rule "perfectionist/sort-imports: off" --rule "perfectionist/sort-named-imports: off" --rule "react-hooks/set-state-in-effect: off" --rule "react-hooks/exhaustive-deps: off" --rule "style/multiline-ternary: off" --rule "style/arrow-parens: off" --rule "max-lines-per-function: off" --rule "antfu/consistent-list-newline: off" --rule "style/jsx-closing-bracket-location: off" --rule "style/brace-style: off"`
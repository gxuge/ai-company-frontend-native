# 任务：快捷登录页前后端对接计划

- 页面：`src/app/pages/quick-login/index.tsx`
- 时间：2026-04-10 10:21
- 当前状态：未对接（手机号展示与一键登录均为静态跳转）
- 预期产出：接入快捷登录接口，登录成功后写入 token 并跳转首页。

## 可复用后端接口
- `POST /sys/phoneLogin`（已封装 `userApi.quickLoginByPhone`）

## 初步字段映射
- 本机号码（脱敏展示）-> `mobile`
- 一键登录按钮 -> `userApi.quickLoginByPhone(mobile)`
- 返回 `token/refreshToken/userInfo` -> 本地鉴权存储

## 任务拆分
| 任务 | 状态 | 说明 |
| --- | --- | --- |
| T1 数据来源接入 | 待开始 | 读取本机手机号（或从缓存账号读取） |
| T2 快捷登录调用 | 待开始 | 绑定一键登录按钮 |
| T3 失败兜底 | 待开始 | 失败时回退到验证码登录页 |
| T4 校验与回填 | 待开始 | eslint + 文档回填 |

## 风险与回退
- 风险：当前前端未接入原生“一键取号 SDK”。
- 回退：无法取号时默认跳转 `/pages/verification-code-login`。

## 证据索引（初始化）
- 前端文件：`src/app/pages/quick-login/index.tsx`
- 前端文件：`src/lib/api/user.ts`


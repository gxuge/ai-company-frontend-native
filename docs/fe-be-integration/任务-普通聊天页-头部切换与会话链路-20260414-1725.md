# 任务-普通聊天页-头部切换与会话链路-20260414-1725

## 任务背景
- 页面：pages/chat
- 目标：在不改布局的前提下，完成普通聊天页的 Header 分流与文本对话链路对接。
- 边界：仅改数据请求、字段映射、路由参数与状态逻辑，不调整视觉结构。
- 非目标：本轮不实现附件上传、语音识别、AI 音频播放/点赞/重试等扩展交互。

## 任务拆分
| 任务 | 状态 | 说明 | 证据 |
| --- | --- | --- | --- |
| T1 会话详情字段接入 | 已完成 | 通过 getSessionDetail 驱动 Header 模式决策 | src/app/pages/chat/index.tsx |
| T2 角色/故事 Header 切换 | 已完成 | 按 storyId/targetRoleId 切换 ChatHeader/ChatRoleHeader | src/app/pages/chat/index.tsx |
| T3 Header 字段映射 | 已完成 | 故事标题/热度、角色名/头像/副标题映射到对应 Header | src/app/pages/chat/index.tsx |
| T4 文本发送链路 | 已完成 | 接入 i-reply，支持输入后发送并回填 AI 回复 | src/app/pages/chat/index.tsx、src/app/pages/chat/components/chat-input/index.tsx |
| T5 灵感建议链路 | 已完成 | 接入 eply-suggestions，点击灯泡填充建议文本 | src/app/pages/chat/index.tsx、src/lib/api/ts-chat.ts |
| T6 API 类型补齐 | 已完成 | TsChatSession 增加 	argetRoleId、sessionStatus；新增建议回复类型 | src/lib/api/ts-chat.ts |

## 字段映射
- 会话详情：
  - storyId -> 故事 Header 决策
  - 	argetRoleId（兜底 oleId）-> 角色 Header 决策
- 故事 Header：
  - 	itle <- TsStory.title | sessionTitle
  - anCount <- TsStory.followerCount（格式化）
- 角色 Header：
  - 
ame <- TsRole.roleName | sessionTitle
  - username <- TsRole.roleSubtitle | occupation
  - vatarSource <- TsRole.avatarUrl | coverUrl
- 发送链路：
  - 入参：sessionId、userContent、historyCount
  - 回参：contentText、ssistantMessageId
- 建议链路：
  - 入参：sessionId、historyCount、userDraft
  - 回参：suggestions[]

## 执行结果
- 已完成：
  - 普通聊天页已实现故事/角色 Header 动态分流。
  - 普通聊天页已实现文本输入后发送，+ 与回车都可触发发送。
  - 普通聊天页已接入建议回复，点击灯泡可回填建议文本到输入区。
  - 历史消息继续按 sessionId 回填，并与新发送消息合并展示。
- 未完成：
  - 附件上传/消息附件链路。
  - 语音识别与语音发送链路。
  - AI 音频播放、重试、点赞等消息操作链路。

## 风险与回退
- 风险：
  - 会话详情缺少 storyId/targetRoleId 时，Header 会降级显示默认值。
  - 上游 AI 异常时，页面会插入失败兜底文案。
- 回退：
  - 回退 src/app/pages/chat/index.tsx、src/app/pages/chat/components/chat-input/*、src/lib/api/ts-chat.ts 即可。

## 验证
- 命令：
  - pnpm exec eslint src/app/pages/chat/index.tsx src/app/pages/chat/components/chat-input/index.tsx src/app/pages/chat/components/chat-input/styles.ts src/lib/api/ts-chat.ts
- 结果：通过。

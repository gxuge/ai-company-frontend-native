# 任务：选择角色页前后端对接计划

- 页面：`src/app/pages/select-role/index.tsx`
- 时间：2026-04-10 10:21
- 当前状态：未对接（角色列表为本地常量）
- 预期产出：接入角色分页查询与关键词搜索，支持选中角色回传。

## 可复用后端接口
- `GET /sys/ts-roles`：角色分页/搜索
- `GET /sys/ts-roles/detail`：角色详情（按需）

## 初步字段映射
- `name` <- `roleName`
- 头像 <- `avatarUrl`
- 筛选关键词 <- `keyword`
- 仅展示可选角色 <- `status=1`（必要时加 `isPublic`）

## 任务拆分
| 任务 | 状态 | 说明 |
| --- | --- | --- |
| T1 页面接入查询接口 | 待开始 | 用 `tsRoleApi.getRoleList` 替换静态 `characters` |
| T2 搜索联动 | 待开始 | 搜索词映射到 `keyword` |
| T3 选择回传 | 待开始 | 通过路由参数或状态管理回传 `roleId` |
| T4 校验与回填 | 待开始 | eslint + 文档回填 |

## 风险与回退
- 风险：角色接口返回头像为空。
- 回退：使用页面默认头像占位图，不影响选择流程。

## 证据索引（初始化）
- 前端文件：`src/app/pages/select-role/index.tsx`
- 后端文件：`TsRoleController.java`


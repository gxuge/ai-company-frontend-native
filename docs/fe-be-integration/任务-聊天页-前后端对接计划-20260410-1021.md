# 任务：聊天页前后端对接计划

- 页面：`src/app/pages/chat/index.tsx`
- 时间：2026-04-10 10:21
- 当前状态：未对接（消息列表与输入流程为静态数据）
- 预期产出：接入会话消息、发送消息、AI 回复与语音播放链路。

## 可复用后端接口
- `GET /sys/ts-chat-messages`：会话消息分页
- `POST /sys/ts-chat-messages`：用户消息落库
- `POST /sys/ts-chat-sessions/ai-reply`：生成 AI 文本+语音
- `POST /sys/ts-chat-sessions/reply-suggestions`：候选回复建议
- `GET/POST /sys/ts-chat-message-attachments`：消息附件

## 初步字段映射
- 气泡文本 <- `contentText`
- AI 音频地址 <- `audioUrl`
- 音频时长 <- `durationSec`
- 发送者类型 <- `senderType`

## 任务拆分
| 任务 | 状态 | 说明 |
| --- | --- | --- |
| T1 API 封装 | 待开始 | 扩展 `ts-chat.ts`（消息、AI 回复、候选回复） |
| T2 页面状态改造 | 待开始 | 接口替换 `messages` 静态数组 |
| T3 输入与发送流程 | 待开始 | 文本发送 -> AI 回复 -> 语音播放 |
| T4 校验与回填 | 待开始 | eslint + 文档回填 |

## 风险与回退
- 风险：麦克风输入链路暂无可用 ASR 接口，需独立补充。
- 回退：本页先仅支持文本输入与语音播放。

## 证据索引（初始化）
- 前端文件：`src/app/pages/chat/index.tsx`
- 后端文件：`TsChatSessionController.java`、`TsChatMessageController.java`


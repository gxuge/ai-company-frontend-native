# 任务：普通聊天页历史消息回填
- 页面：`src/app/pages/chat/index.tsx`
- 时间：2026-04-13 19:45
- 当前状态：已完成
- 目标：进入普通会话后，根据 `sessionId` 拉取并展示历史消息，不改动现有布局。

## 任务拆分
| 任务 | 状态 | 说明 |
| --- | --- | --- |
| T1 路由参数接入 | 已完成 | `useLocalSearchParams` 读取 `sessionId` |
| T2 历史消息加载 | 已完成 | 调用 `/sys/ts-chat-messages` 获取消息列表 |
| T3 页面消息映射 | 已完成 | 将后端消息映射到 `ChatAi/ChatUser` 组件数据结构 |

## 页面字段到接口字段映射
- `ai.name <- TsChatMessage.senderName`（缺失回退“系统”）
- `ai.speechText <- TsChatMessage.contentText`
- `user.segments[0].text <- TsChatMessage.contentText`
- `message.type <- TsChatMessage.senderType`（`user` -> 用户气泡，其他 -> AI 气泡）

## 调用时机与失败回退
- 首次进入页面且 `sessionId` 有效时调用：
  - `GET /sys/ts-chat-messages?sessionId={id}&pageNo=1&pageSize=100`
- 后端倒序数据在前端翻转为正序展示。
- 接口失败时保持默认展示数据，避免页面空白。

## 执行结果
- 完成项：
  - 普通聊天页已可按会话历史回填。
  - UI 结构和布局未调整，仅替换数据来源。
- 未完成项：无
- 风险：
  - 当前仍未接入“发送消息落库”链路，本轮只完成历史读取。

## 证据索引
- `src/app/pages/chat/index.tsx`
- `src/lib/api/ts-chat.ts`

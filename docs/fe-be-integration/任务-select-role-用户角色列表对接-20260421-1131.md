# 任务-select-role-用户角色列表对接-20260421-1131

## 任务背景
- 页面：`pages/select-role`
- 目标：将页面静态角色数组切换为“当前用户角色列表”真实接口数据。
- 边界：仅改接口调用、状态与字段映射，不改动现有 UI 布局与视觉结构。
- 非目标：不新增后端接口，不改动 `pages/create-story` 页面结构。

## 任务拆分（执行前计划）
| 任务 | 状态 | 说明 | 验收标准 | 证据 |
| --- | --- | --- | --- | --- |
| T1 现状与契约核对 | 已完成 | 确认 `select-role` 使用静态数组，接口复用 `tsRoleApi.getRoleList` | 数据缺口与映射关系清晰 | `src/app/pages/select-role/index.tsx`、`src/lib/api/ts-role.ts` |
| T2 页面列表对接 | 已完成 | 替换静态 `items`，改为拉取用户角色列表并本地搜索过滤 | 页面展示真实角色数据 | `src/app/pages/select-role/index.tsx` |
| T3 选择回传保持 | 已完成 | 保持 `from=create-story` 回传 `selectedRoleId/Name/Avatar` 行为 | 返回创建故事页可接收角色参数 | `src/app/pages/select-role/index.tsx` |
| T4 校验与文档回填 | 已完成 | 定向 lint 与任务文档、总表、计划、日志回填 | 记录完整可追溯 | `pnpm exec eslint ...`、`docs/**` |

## 字段映射（计划）
- `item.id <- role.id`
- `item.name <- role.roleName`
- `item.avatar <- role.avatarUrl || role.coverUrl || 本地占位图`
- 回传 `selectedRoleAvatar <- role.avatarUrl || role.coverUrl || ''`

## 风险与回退（计划）
- 风险：角色列表接口失败时页面为空态。
- 回退：保留空态与错误文案，不破坏页面结构；必要时可快速回退本页文件。

## 执行结果（2026-04-21）
- 已完成：
  - 页面从静态角色数组切换到 `tsRoleApi.getRoleList` 实时加载。
  - 新增页面内加载态与失败文案回退（不改布局结构）。
  - 搜索逻辑改为对后端返回角色做本地过滤。
  - 维持 `from=create-story` 的回传链路，回传字段为 `selectedRoleId/selectedRoleName/selectedRoleAvatar`。
- 验证：
  - 已执行：`pnpm exec eslint src/app/pages/select-role/index.tsx`
  - 结果：0 error，存在 tailwind 规则类 warning（历史样式规范类问题，不阻断本次联调）。

## 风险与备注（执行后）
- 后端 `hardness-writing-spec.md` 在 `D:\project_demo\ai-company-backend-spring\jeecg-boot\docs\` 下未找到，本轮按现有前端对接规范执行。

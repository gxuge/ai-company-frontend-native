# 任务：会话列表页前后端对接计划

- 页面：`src/app/pages/session-list/index.tsx`
- 时间：2026-04-10 10:21
- 当前状态：已对接（本轮完成会话列表真实数据渲染）
- 预期产出：完成会话列表真实数据渲染，保留原 UI 布局。

## 可复用后端接口
- `GET /sys/ts-chat-sessions`：会话分页列表
- `GET /sys/ts-chat-sessions/detail`：会话详情（按需）
- `GET /sys/ts-chat-messages`：按会话拉取消息（用于最近一条预览）

## 字段映射（已落地）
- `name` <- `sessionTitle`
- `message` <- 最近一条消息 `contentText`
- `time` <- `lastMessageAt`
- `badge` <- `unreadCount`（缺失时回退为 `0`）

## 任务拆分
| 任务 | 状态 | 说明 |
| --- | --- | --- |
| T1 API 封装 | 已完成 | 新增 `src/lib/api/ts-chat.ts`（会话/消息查询）并更新统一导出 |
| T2 页面列表改造 | 已完成 | 用接口替换静态 `conversations`，首屏加载会话并拉取最近消息 |
| T3 分类角标策略 | 已完成（占位） | 分类图标区保留原 UI，不依赖新增统计接口 |
| T4 校验与回填 | 已完成 | 执行 eslint 并回填文档 |

## 风险与回退
- 风险：后端无“分类角标统计”专用接口。
- 回退：角标显示 `--` 或 `0`，不阻断主列表渲染。

## 调用时机与失败回退（本轮）
- 页面首次加载：调用 `GET /sys/ts-chat-sessions` 获取会话列表。
- 会话消息预览：按会话并发调用 `GET /sys/ts-chat-messages`（`pageNo=1&pageSize=1`）取最近一条文本。
- 会话列表失败：展示错误文案，不改动页面布局结构。
- 单会话消息失败：该会话消息文案回退为“暂无消息”，不阻断其它会话渲染。

## 执行结果（2026-04-10）
- 完成项：
  - 会话列表页已切换为后端真实数据。
  - 新增 `ts-chat` 业务 API 封装并接入统一导出。
  - 页面增加加载态、错误态、空态文案（仅文本，不改布局）。
- 进行中：
  - 无。
- 未完成项：
  - 无。
- 风险：
  - 分类统计接口缺失，当前保留占位策略。

## 证据索引（本轮）
- 前端文件：`src/lib/api/ts-chat.ts`
- 前端文件：`src/lib/api/index.ts`
- 前端文件：`src/app/pages/session-list/index.tsx`
- 验证命令：`pnpm exec eslint src/app/pages/session-list/index.tsx src/lib/api/ts-chat.ts src/lib/api/index.ts`

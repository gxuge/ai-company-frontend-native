# 任务：创建故事页前后端对接计划

- 页面：`src/app/pages/create-story/index.tsx`
- 时间：2026-04-10 10:21
- 当前状态：未对接（故事配置、章节配置均为本地状态）
- 预期产出：完成故事保存、一键生成与章节保存链路。

## 可复用后端接口
- `POST/PUT /sys/ts-stories`：故事保存
- `POST /sys/ts-stories/story-setting-generate`：故事设定生成
- `POST /sys/ts-stories/story--scene-generate`：场景生成
- `POST /sys/ts-stories/story--outline-generate`：大纲生成
- `POST/PUT /sys/ts-story-chapters`：章节保存
- `GET /sys/ts-roles`：可选角色列表

## 初步字段映射
- 标题/简介/故事设定 -> `TsStorySaveDto`
- 章节标题/开场白/任务目标 -> `TsStoryChapterSaveDto`
- 角色选择 -> `roleBindings[].roleId`

## 任务拆分
| 任务 | 状态 | 说明 |
| --- | --- | --- |
| T1 API 封装 | 待开始 | 扩展 `ts-story.ts`（create/update/generate/chapter） |
| T2 页面主表单接入 | 待开始 | 接入故事保存与草稿更新 |
| T3 章节与角色接入 | 待开始 | 章节列表与角色绑定落库 |
| T4 校验与回填 | 待开始 | eslint + 文档回填 |

## 风险与回退
- 风险：场景主数据（`sceneId/sceneNameSnapshot`）当前页面缺来源。
- 回退：先允许为空并用文本描述字段兜底。

## 证据索引（初始化）
- 前端文件：`src/app/pages/create-story/index.tsx`
- 后端文件：`TsStoryController.java`、`TsStoryChapterController.java`


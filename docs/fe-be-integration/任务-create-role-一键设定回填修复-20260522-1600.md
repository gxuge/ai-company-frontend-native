# 任务：create-role 一键设定回填修复（2026-05-22 16:00）

## 1. 背景与目标
- 目标：
  - 修复 `POST /sys/ts-roles/one-click-setting` 在“未传角色名”时后端自动补 `角色+时间戳` 的行为。
  - 修复“一键设定已生成但前端未正确回填”问题，确保四核心字段稳定回填到 `create-role` 页面。
- 边界：
  - 仅改字段映射与生成回填逻辑，不改页面 UI 布局与样式结构。
- 非目标：
  - 不改动角色保存接口 `POST/PUT /sys/ts-roles` 的业务规则。

## 2. 任务拆分
| 任务 | 状态 | 说明 | 验收标准 | 证据 |
| --- | --- | --- | --- | --- |
| T1 后端角色名默认值修正 | 已完成 | 去除 one-click-setting 的 `角色+时间戳` 默认名 | 前端不传 `roleName` 时，后端不再强行生成默认名 | `TsRoleGenerateServiceImpl.java` |
| T2 后端字段解析兼容 | 已完成 | 支持 snake/camel 双键读取（`role_name/roleName`、`background_story/backgroundStory`） | 模型返回字段可被稳定解析 | `TsRoleGenerateServiceImpl.java` |
| T3 前端回填兼容 | 已完成 | `create-role` 回填支持 snake/camel 双返回键 | 页面角色名/职业/背景可稳定回填 | `create-character.tsx` |
| T4 前端类型补齐 | 已完成 | API 结果类型补充 `role_name`、`background_story` | 不依赖 `any` 访问关键回填字段 | `ts-role.ts` |

## 3. 字段映射（本轮口径）
- 页面 `name` <- `result.roleName || result.role_name`
- 页面 `gender` <- `result.gender`
- 页面 `job` <- `result.occupation`
- 页面 `background` <- `result.backgroundStory || result.background_story`

## 4. 调用时机与失败回退
- 调用时机：点击 `create-role` 页面“一键生成设定”按钮时调用 `POST /sys/ts-roles/one-click-setting`。
- 失败回退：接口失败时仅提示错误文案，不改动用户当前输入状态。

## 5. 执行结果
- 完成项：
  - 后端 one-click-setting 已去除角色名时间戳兜底。
  - 后端解析模型输出改为 snake/camel 双兼容。
  - 前端回填逻辑改为 snake/camel 双兼容。
  - 前端 API 类型已补齐返回字段别名。
- 未完成项：
  - 无。
- 风险：
  - 若模型输出出现非 schema 字段（例如中文键名），仍可能触发后端兜底值。

## 6. 证据索引
- 后端：`D:\project_demo\ai-company-backend-spring\jeecg-boot\jeecg-module-system\jeecg-system-biz\src\main\java\org\jeecg\modules\system\service\impl\TsRoleGenerateServiceImpl.java`
- 前端：`src/app/pages/create-role/components/create-character.tsx`
- 前端：`src/lib/api/ts-role.ts`

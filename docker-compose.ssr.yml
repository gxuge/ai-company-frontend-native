services:
  web-ssr:
    build:
      context: .
      dockerfile: Dockerfile.ssr
    container_name: ai-company-frontend-web-ssr
    ports:
      - "${WEB_SSR_PORT:-8081}:8081"
    environment:
      NODE_ENV: production
    restart: unless-stopped